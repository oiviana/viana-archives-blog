import NotFoundContent from "@/components/NotFoundContent"

export default function NotFound() {
  return (
    <NotFoundContent
      title="Página não encontrada"
      description="O endereço acessado não existe ou foi movido. Volte para a página principal e continue navegando pelos arquivos."
    />
  )
}
